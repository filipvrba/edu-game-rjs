export default class TFxTeleport < Tile
  attr_accessor :id_tile

  def initialize
    super Vector2.new(20, 16)

    @id_tile = 0
  end

  def ready()
    @img = this.get_scene(true).find_child('images').fx_teleport
  end
end
